// This page is displayed if any other user tries to view a poll

import { useParams } from "react-router";

function PollPage() {
  return <div>You can view this poll but you did not create it</div>;
}

export default PollPage;
